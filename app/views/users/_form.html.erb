<%= simple_form_for user, remote: true do |f| %>
  <%= f.error_notification %>
  <%= f.input :first_name, placeholder: "First name", error: "mettez votre prénom"  %>
  <%= f.input :last_name, placeholder: "Last name", error: "mettez votre nom"  %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
